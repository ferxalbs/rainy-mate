# Beam Deployment Guardrails

- Never trust a single opaque reporter forever; rotate and monitor reporter keys.
- Keep large AI payloads offchain and anchor only digests or URIs onchain.
- Preserve idempotent fulfillment rules so requests cannot be overwritten silently.
- If using paid oracle jobs, charge or sponsor explicitly instead of leaking balances.
